fastapi>=0.110.0
uvicorn[standard]>=0.27.0
requests>=2.31.0
pandas>=2.2.0
python-dotenv>=1.0.0
supabase>=2.5.0
httpx>=0.27.0

# 프리미어 XML 교차 검증 — 내가 쓴 XML 을 **독립 구현**에 읽혀 본다
# (scripts/check_premiere.py). 없으면 그 단계만 건너뛴다
opentimelineio>=0.17
otio-fcp-adapter>=1.0
