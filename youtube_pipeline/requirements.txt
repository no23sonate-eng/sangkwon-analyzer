# youtube_pipeline 전용 의존성 (루트 requirements.txt 와 분리)
# 시스템 의존성: ffmpeg (apt/brew 로 별도 설치)
faster-whisper>=1.0.0
anthropic>=0.40.0
python-dotenv>=1.0.0
PyYAML>=6.0
requests>=2.31.0
# 한국어 띄어쓰기 교정 (transcribe.fix_spacing)
kiwipiepy>=0.18.0

# 로컬 웹앱 (app.py)
flask>=3.0

# 소스 라이브러리 (assets_lib.py — 픽토그램/지도)
cairosvg>=2.7
matplotlib>=3.8
Pillow>=10.0
